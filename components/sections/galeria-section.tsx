'use client';
// This section is now integrated into ExperienciaSection as a photo strip.
// Keeping this file as a re-export to avoid breaking imports.

export function GaleriaSection() {
  return null;
}
